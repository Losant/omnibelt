{
  "name": "losant-utils",
  "version": "1.1.1",
  "private": true,
  "main": "dist/index.js",
  "repository": "git@github.com/Losant/losant-utils.git",
  "author": "@Losant",
  "engines": {
    "node": ">=8.9.4",
    "yarn": ">=1.3.2"
  },
  "scripts": {
    "preinstall": "losant-scripts force-yarn 2> /dev/null || npx @losant/scripts force-yarn",
    "reinstall": "rm -rf node_modules && yarn install",
    "build": "rm -rf dist/* && babel src --out-dir dist --ignore *.test.js --no-comments",
    "lint": "esw *.js src",
    "lint:fix": "yarn lint --fix",
    "lint:watch": "yarn lint --watch",
    "lint:staged": "lint-staged",
    "test": "jest --verbose",
    "test:watch": "yarn test --watch -o",
    "precommit": "lint-staged",
    "prepublish": "yarn build",
    "postmerge": "losant-scripts git-hook post-merge",
    "postversion": "losant-scripts git-hook post-version"
  },
  "lint-staged": {
    "*.js": "esw"
  },
  "dependencies": {
    "@rjhilgefort/export-dir": "^1.1.0",
    "lodash": "^4.17.4",
    "ramda": "^0.24.1",
    "string-format": "^0.5.0",
    "stringify-object": "^3.2.1"
  },
  "devDependencies": {
    "@losant/eslint-config-losant": "^1.2.3",
    "@losant/scripts": "^1.0.10",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-plugin-transform-imports": "^1.4.1",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "fs-extra": "^4.0.3",
    "husky": "^0.14.3",
    "jest": "^21.0.2",
    "lint-staged": "^6.0.0",
    "present": "^1.0.0"
  },
  "eslintConfig": {
    "extends": "@losant/eslint-config-losant/base",
    "env": {
      "jest": true
    },
    "rules": {
      "arrow-body-style": 0,
      "valid-jsdoc": 0
    }
  }
}
