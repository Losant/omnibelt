{
  "name": "losant-utils",
  "version": "0.0.1",
  "main": "src/index.js",
  "repository": "git@bitbucket.org:losant/losant-utils.git",
  "author": "@Losant",
  "main": "src/index.js",
  "engines": {
    "node": "6.11.2",
    "yarn": "0.27.5"
  },
  "scripts": {
    "preinstall": "node -e 'if(process.env.npm_lifecycle_script){console.error(`Use yarn!`);process.exit(1);}'",
    "lint": "eslint *.js src",
    "lint:fix": "eslint --fix *.js src",
    "test": "jest --watch -o --verbose"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "ramda": "^0.24.1"
  },
  "devDependencies": {
    "eslint-config-losant": "git+ssh://git@bitbucket.org/losant/eslint-config-losant.git#v1.1.22",
    "jest": "^20.0.4"
  },
  "eslintConfig": {
    "extends": "losant/env/node",
    "env": {
      "jest": true
    }
  }
}
